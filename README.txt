(c) E44, MCsharerGIT, Blockmen3000
Moin, das hier wird (vielleicht) ein Programm mit dem der XArm 5 von uFactory ein Bild mit einem Stift an eine Tafel malen kann.

zu installierende benötigte Bibliotheken (nicht vollständig):
- numpy
- opencv
- pillow

cmd-Befehle (nicht vollständig):
pip install numpy
pip install opency-python
pip install pillow